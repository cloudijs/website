name: Deploy
on:
  push:
    branches:
      - main
    paths:
      - 'dist/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Upload website to Bunny
    container:
      image: sbstjn/bunsy:latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Upload to Bunny storage
        run: bunsy ./dist $BUNNY_STORAGE_ZONE_NAME
        env:
          BUNNY_STORAGE_ZONE_NAME: ${{ secrets.BUNNY_STORAGE_ZONE_NAME }}
          BUNNY_STORAGE_ZONE_PASSWORD: ${{ secrets.BUNNY_STORAGE_ZONE_PASSWORD }}
